function GetAllBright(e) {
    // body...
    let cvs = this.canvas
    let ctx = this.context
    let position = this.windowToCanvas(e.clientX,e.clientY)
    let areas = Object.values(this.areas)
    let windows = Object.values(this.chuangs)
    let wall = Object.values(this.wall)
    getLightSideNum(wall)
    if(target.claass == 'room'){
        if(target.claass != 'wall' || target.claass != 'chuangs'){
            for(let i = 0;i<){
                
            }
            ctx.fillRect(i,j,1,1)
        }
        
    }
}